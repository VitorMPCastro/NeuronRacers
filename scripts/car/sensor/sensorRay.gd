extends Resource
class_name SensorRay

@export var enabled: bool = true
@export var angle_deg: float = 0.0           # angle relative to +X (car forward), degrees
@export var max_length_px: float = 1000000
